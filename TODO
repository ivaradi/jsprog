- repeatDelay editing for a SimpleAction
- handle the Super key in a SimpleAction as a modifier
- simplification if the handlertree when clearing an action
- the version should not be compulsory for a profile, but should be usable
  as a filter
- the joystick context menus should contain items to copy the version,
  the physical location and the unique identifier to the currently
  edited profile
- call on the D-Bus interface to quit
- sanity check on virtual control and shift state editing
